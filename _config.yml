# Site settings
title: Brandon Finley
description: Glaciology · Ice flow modeling · Scientific computing
url: "https://brandonfinley.me"
baseurl: ""

# Build settings
markdown: kramdown
kramdown:
  math_engine: mathjax   # leaves $$ ... $$ blocks alone so MathJax can render them

# Default layout for all pages
defaults:
  - scope:
      path: ""
    values:
      layout: default
